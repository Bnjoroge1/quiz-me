Generate a quiz about the current task and code.

Run `quiz-me serve`, then `quiz-me generate --scope uncommitted --difficulty mix`.
Generate the quiz JSON per the prompt using your model, POST to the printed endpoint, and share the browser URL.
